# log frequency
val_check_interval: 1

wandb:
  name: ${expname}
  project: ${project}
  entity: null
  log_model: True
  mode: 'offline'
  group: ${expname}

wandb_watch:
  log: 'all'
  log_freq: 500

lr_monitor:
  logging_interval: "step"
  log_momentum: False

draw_predictions: False
